'use strict';

describe('test', function initTests()
{
    it('success', function()
    {
        expect(1).to.equal(1);
    });

    it('failure', function()
    {
        expect(1).to.equal(0);
    });
});
